RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx
CONTACT_TO_EMAIL=hello@nexaedubiz.com
CONTACT_FROM_EMAIL=NexaEduBiz <onboarding@resend.dev>

# Auth: random string 32+ characters (openssl rand -base64 32). Required in production.
AUTH_SECRET=

# Neon Postgres connection string (use the Pooled connection from Neon dashboard).
# Format: postgresql://USER:PASSWORD@HOST/DB?sslmode=require
DATABASE_URL=
